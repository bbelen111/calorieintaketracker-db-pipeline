# Secrets
.env
.env.local

# Databases / binaries (all build outputs stay local and uncommitted)
*.sqlite
*.db
*.sqlite-journal

# Raw USDA bulk dumps (drop them into fdc-download/ locally)
fdc-download/

# Dependencies
node_modules/

# Auto-generated audit/anomaly/quarantine dumps (keep the directory via .gitkeep)
reports/*.json
!reports/.gitkeep